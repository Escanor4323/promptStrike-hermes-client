<!--
  LoadingSpinner.svelte — Loading Indicator
  ─────────────────────────────────────────────────────────────
  Renders:
    A centered loading spinner animation. Used across the app
    during async data fetches and form submissions.

  Props:
    - size: 'sm' | 'md' | 'lg' — spinner size (default: 'md')
    - message: string | null — optional loading message below spinner

  Events:
    (none — display only)
-->
